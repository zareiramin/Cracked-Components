This example implements a Web service wrapper for a Modbus Master.

To use it load the project MMBWebService.csproj into Visual Studio 2005

and compile and run this project.

Load the http://localhost:????/default.aspx page into a web browser and press the start link.

where ???? is the port number the web server is running on.


